# BRINKS RouteMaster — אפיון מלא + תוכנית פיתוח ל-MVP
### מערכת אופטימיזציית סבבים לראשי צוותים | מסמך ארכיטקטורה v2.0 — מלא

**מה יש כאן:** אפיון פונקציונלי מלא, מערכת עיצוב, ו-5 פרומפטי Cursor שמובילים מאפס ל-MVP עובד ומוכן לפיילוט.

| חלק | תוכן |
|---|---|
| **א'** | אפיון פונקציונלי — הבעיה, הזרימה, המנוע, הלמידה, השעון, הסיכונים, הפריסה, מדדי ההצלחה |
| **ב'** | מערכת העיצוב "Vault" — צבעים, טיפוגרפיה, אפקטים, אלמנט החתימה |
| **ג'** | קובץ `.cursorrules` — החוזה ההתנהגותי של קורסור |
| **ד'–ח'** | 5 פרומפטי פיתוח: Foundation → Data Core → Route Engine → Live Mode → Intelligence |
| **ט'** | מה עושים אחרי ה-MVP — הדרך למכירה |

⚠️ **איך להשתמש:** **התחל מסעיף 14 (Phase 0) — לא מ-Phase 1.** שבוע של אימות לפני חודשים של פיתוח. אחר כך: הרץ פאזה אחת, בדוק מול ה-Completion Criteria, חזור אליי עם התוצאה — ואז נמשיך. הפרומפטים כאן הם מפת הדרכים, לא מרוץ.

---

# חלק א' — אפיון פונקציונלי (עברית)

## 1. תמצית הפרויקט

אפליקציית Web (PWA) לראש צוות בברינקס, שמקבלת רשימת יעדים יומית ובונה את סדר הנסיעה האופטימלי — לא "מהקרוב לרחוק" אלא **המסלול שמסיים את כל הסבב (כולל חזרה לסניף) הכי מהר**, תוך עמידה באילוצי זמן, עדיפויות, פקקים בזמן אמת, והפסקות.

**משתמש היעד:** ראש צוות בודד ברכב (עם צוותו). פיילוט עם ראש צוות אחד, ארכיטקטורה מוכנה לריבוי משתמשים ודשבורד ניהולי (שלב 2).

## 2. החלטות אפיון (סיכום התשובות)

| נושא | החלטה |
|---|---|
| היקף סבב | 10–25 יעדים ביום |
| מבנה | ראש צוות נוסע בעצמו, רכב אחד |
| מקור נתונים | מערכת Zebra (אין גישת API כרגע) → קלט גמיש: ידני / קובץ / צילום-מסך עם OCR |
| אילוצי זמן | דד-ליין, "משעה X", חלון X–Y, עדיפות VIP — הכל, בבחירה ידנית פר-יעד |
| התנגשויות | המערכת מתריעה ומציעה פתרונות — ראש הצוות מחליט |
| משך שירות | משתנה פר-לקוח, מוגדר ידנית + למידה מהיסטוריה בעתיד |
| זיהוי הגעה | גיאופנס אוטומטי + אישור ידני ("הגעת? סמן בוצע") |
| פלטפורמה | PWA רספונסיבי (רץ על Zebra/Android, iPhone, דסקטופ) |
| דשבורד ניהולי | שלב 2 — אבל כל הנתונים נשמרים בשרת מהיום הראשון |
| הפסקות | הפסקה מוגדרת (משך) + שיבוץ חכם ע"י המערכת |
| גיוון מסלול (אבטחה) | מתג ניתן להפעלה — לברר מול הלקוח |
| שעות | יציאה נבחרת (06:30/07:00/09:00/מותאם), חזרה גמישה עם צפי מחושב |
| סניף | אחד קבוע — נקודת יציאה וחזרה |
| עיצוב | השראת ברינקס (נייבי+זהב) בעיצוב מודרני פרימיום, עברית RTL |
| יעד | MVP עובד אמיתי לפיילוט |

## 3. זרימת עבודה יומית (User Journey)

**בוקר — תכנון (5 דקות):**
1. ראש הצוות פותח את האפליקציה → מסך "סבב חדש".
2. מזין יעדים באחת מ-3 דרכים: הקלדה ידנית עם השלמת כתובות / העלאת קובץ (Excel/CSV) / צילום מסך של אפליקציית Zebra → AI מחלץ כתובות אוטומטית.
3. לכל יעד: כתובת, שם לקוח, אילוץ זמן (אופציונלי), עדיפות VIP (אופציונלי), משך שירות (ברירת מחדל או מותאם), הערות.
4. בוחר שעת יציאה + משך הפסקת צהריים רצוי.
5. לוחץ "חשב מסלול" → תוך שניות מקבל: סדר יעדים ממוספר על מפה + רשימה, צפי הגעה לכל יעד, שיבוץ הפסקה, צפי חזרה לסניף.
6. אם יש התנגשות אילוצים → באנר אזהרה עם הסבר + 2–3 הצעות פתרון.
7. עריכה ידנית: גרירת סדר, נעילת יעד למיקום, הוספה/הסרה → חישוב מחדש מיידי.
8. "התחל סבב".

**במהלך היום — מצב נסיעה (Live Mode):**
- מסך גדול וברור: היעד הנוכחי, ETA, כפתור ענק "נווט בוויז" (deep link).
- GPS מזהה הגעה (רדיוס ~150 מ') → התראה "הגעת ל[לקוח]? סמן בוצע" → מעבר אוטומטי ליעד הבא.
- ציר התקדמות: בוצע ✓ / נוכחי / הבאים, עם צפי מתעדכן לפי תנועה בפועל.
- סטייה מהצפי (איחור מצטבר) → המערכת מציעה חישוב מחדש.
- הוספת יעד תוך כדי יום → שיבוץ חכם מחדש.
- תזכורת הפסקה בזמן/מקום שנקבע.
- כפתור SOS מהיר (חיוג למוקד).

**סוף יום:**
- הגעה לסניף → סיכום יומי אוטומטי: יעדים שבוצעו, זמנים בפועל מול צפי, ק"מ, זמן שנחסך מול מסלול "נאיבי" (לפי סדר ההזנה) — קלף המכירה שלך מול ברינקס.
- שמירה בהיסטוריה + "שכפל סבב" למחר.

## 4. מנוע האופטימיזציה — הלב של המערכת

- **למה "מהקרוב לרחוק" נכשל:** האלגוריתם הנאיבי (Nearest Neighbor) בוחר בכל צעד את היעד הקרוב ביותר — ואז מגלה בסוף היום שנשאר לו יעד יחיד בקצה השני של המפה, ונוסע 40 דקות סרק. בבעיות כאלה הוא סוטה בממוצע ב-20–25% מהאופטימום. אנחנו לא בונים "סדר לפי מרחק" — אנחנו **פותרים בעיית אופטימיזציה גלובלית**: מתוך כל הסדרים האפשריים (ל-20 יעדים יש 20! ≈ 2.4 קוודריליון אפשרויות), למצוא את זה שמסיים את **כל** היום — כולל החזרה לסניף — הכי מוקדם, בלי להפר אף אילוץ.

- **מודל הבעיה:** VRPTW חד-רכבי (Vehicle Routing Problem with Time Windows) עם depot קבוע להתחלה וסיום, אילוצי חלונות זמן, משכי שירות משתנים, הפסקה, ועדיפויות. נפתר ב-**Google OR-Tools** — פותר תעשייתי חינמי שמשמש חברות לוגיסטיקה גלובליות. ל-25 יעדים: פתרון איכותי תוך 2–5 שניות.

### איך המנוע עובד — 5 שלבים

**שלב 1 — מטריצת נסיעה מודעת-זמן (Time-Dependent Matrix).**
זה ההבדל בין מערכת חובבנית למקצועית: זמן נסיעה מ-א' ל-ב' ב-07:15 שונה לחלוטין מאותה נסיעה ב-11:00. לכן לא בונים מטריצה אחת — בונים **3 מטריצות** לפי חלונות זמן (בוקר-פקקים / אמצע-יום / אחה"צ-פקקים), כל אחת דרך Google Routes API עם `departureTime` מתאים ו-`trafficModel: BEST_GUESS`. הפותר בוחר את המטריצה הרלוונטית לפי השעה החזויה של כל מקטע. עלות: 3 קריאות Matrix בבוקר (זניח), רווח: הערכות שמשקפות פקקים אמיתיים.
→ כל ערך במטריצה מוכפל ב**מקדם הלמידה** הנלמד למקטע (סעיף 4.5), אם קיים.

**שלב 2 — בניית מודל האילוצים.**
| רכיב | מימוש ב-OR-Tools |
|---|---|
| ממד זמן | `AddDimension` עם slack (המתנה מותרת), קיבולת = אורך יום מקסימלי |
| "עד שעה X" (דד-ליין) | `CumulVar(index).SetMax(X)` |
| "משעה X ואילך" | `CumulVar(index).SetMin(X)` — עם slack, כלומר מותר להגיע מוקדם ולהמתין |
| חלון X–Y | `SetRange(X, Y)` |
| משך שירות | נוסף לזמן הנסיעה היוצא מכל צומת (`transit_callback`) |
| חזרה לסניף | הצומת האחרון = depot; **פונקציית המטרה ממזערת את זמן ההגעה אליו** |
| VIP | ראה שלב 3 |
| הפסקה | ראה שלב 4 |

**שלב 3 — פונקציית המטרה: מה בדיוק אנחנו ממזערים.**
```
Minimize:  makespan (שעת החזרה לסניף)
         + Σ penalty_deadline_risk(stop)     # קנס גדל ככל שההגעה קרובה לדד-ליין
         + Σ vip_lateness_weight × arrival_time(vip_stop)
         + drop_penalty × dropped_stops      # רק כשאין פתרון חוקי
```
- **המטרה הראשית היא זמן הסיום, לא סך הק"מ.** זו הגדרה שונה מהותית: לפעמים כדאי לנסוע 5 ק"מ יותר כדי להימנע מ-20 דקות פקק או כדי לתפוס לקוח לפני הדד-ליין שלו.
- **VIP = משקל, לא כלל ברזל:** ה-VIP נדחף להיות מוקדם ככל האפשר, אבל אם הקדמתו תוסיף 40 דקות ליום — המנוע יעדיף להשאיר אותו במקומו הטבעי ולהתריע. משקל ה-VIP חשוף כהגדרה (`vip_weight`) שאפשר לכייל אחרי הפיילוט.
- **חיץ בטיחות:** לכל דד-ליין מוסיפים buffer של 10 דקות (ניתן להגדרה) — כדי שהתוכנית לא תהיה "בדיוק בדקה" ותקרוס מכל עיכוב קטן.

**שלב 4 — ההפסקה כצומת וירטואלי.**
ההפסקה לא נדחפת אחרי החישוב — היא **חלק מהאופטימיזציה עצמה**: צומת וירטואלי עם משך = משך ההפסקה שהוגדר, חלון זמן 11:30–14:00 (ניתן להגדרה), וזמן נסיעה 0 מכל נקודה (כלומר: היא מתרחשת בין שני יעדים, בלי לגרור סטייה). התוצאה: המערכת בוחרת את **הרגע הזול ביותר** להפסקה — למשל בדיוק כשממילא צריך להמתין ללקוח שנפתח ב-13:00. ב-OR-Tools זה `AddDisjunction` על צומת עם חלון זמן, או מנגנון `BreakIntervalVar` המובנה בממד הזמן.

**שלב 5 — פתרון, אימות והסבר.**
- אסטרטגיה: `PATH_CHEAPEST_ARC` לפתרון התחלתי → `GUIDED_LOCAL_SEARCH` (מטא-היוריסטיקה שיודעת "לברוח" מאופטימום מקומי) עם תקציב זמן של 5 שניות.
- **אם אין פתרון חוקי** (התנגשות אילוצים): המנוע לא נכשל בשקט. הוא מריץ שוב עם `AddDisjunction` וקנסות — כלומר "מותר לוותר על יעד בעלות גבוהה" — ומזהה **בדיוק איזה יעד גורם לבעיה**. הממשק מציג: "לא ניתן להספיק את [לקוח א'] עד 10:00 ואת [לקוח ב'] עד 10:30 — הם במרחק 35 דק' זה מזה. אפשרויות: (1) לדחות את ב' ל-11:15 (2) להעביר את א' למחר (3) לוותר על חיץ הבטיחות."
- **שקיפות = אמון:** לכל מסלול המערכת מציגה למה הוא נבחר — "סדר זה חוסך 47 דקות מול הסדר שהוזן" + סימון היעדים שקבעו את המבנה (אילוצי הזמן). ראש צוות שמבין את ההיגיון — סומך על המערכת ולא עוקף אותה.

### חישוב מחדש חכם תוך כדי יום (Re-optimization)
זה מה שהופך את זה מ"מתכנן" ל"שותף":
- יעדים שכבר בוצעו **נעולים כהיסטוריה** — הפותר מקבל את המצב הנוכחי (מיקום GPS, שעה, יעדים שנותרו) ופותר מחדש רק את מה שנשאר.
- טריגרים לחישוב מחדש: הוספת יעד תוך כדי יום, ביטול/דילוג יעד, איחור מצטבר מעל 15 דקות, או בקשה ידנית.
- **אף פעם לא משנה סדר מאחורי הגב:** המערכת מציגה הצעה — "זוהה עיכוב של 22 דק'. סידור מחדש יחסוך 18 דק' ויציל את הדד-ליין של [לקוח]. להחיל?" — וראש הצוות מאשר. יעדים ש"ננעלו" ידנית נשארים במקומם תמיד.

### מצב גיוון (אבטחה)
כשהמתג פעיל: הפותר מייצר 5 פתרונות טובים (`solution_limit`), מסנן את אלה שסוטים ≤7% מהאופטימום, ובוחר אקראית מביניהם — עם העדפה לסדר ששונה מהסדר של אתמול (השוואה מול היסטוריה). התוצאה: אי-חיזוי אמיתי בעלות זניחה של דקות ספורות.

- **Waze:** אין API לאופטימיזציה — משמש רק לניווט בפועל דרך deep link (`waze://?ll=lat,lon&navigate=yes`). כל הלוגיקה על Google Maps Platform.

## 4.5 מנוע הלמידה — המערכת שמשתפרת כל יום 🧠

זה היתרון התחרותי האמיתי: אחרי שבועיים של שימוש, ההערכות של המערכת מדויקות יותר מכל מתחרה, כי היא למדה את המציאות של ברינקס — לא הערכות גנריות של גוגל.

**מה נלמד ואיך:**

1. **משך שירות אמיתי פר-לקוח:**
   - הגיאופנס מתעד אוטומטית: כניסה לרדיוס = `actual_arrival`, יציאה מהרדיוס (או סימון "בוצע" + תזוזה) = `actual_departure`.
   - ההפרש = דגימת משך שירות אמיתית: מהרגע שירדו מהרכב, אספו את הכסף, ועד שהמשיכו ליעד הבא.
   - **אפס עבודה ידנית** — ראש הצוות לא מקליד כלום, המערכת מתעדת לבד.
   - כל לקוח מזוהה ברשומת `customer` קבועה (לפי כתובת מנורמלת + שם) שצוברת דגימות.
   - אחרי 3+ ביקורים: ההערכה עוברת מברירת המחדל לחציון הנלמד (חציון עמיד לחריגים — פעם אחת שהיה בלגן לא הורסת את הממוצע). משקל גבוה יותר לביקורים אחרונים (EWMA).
   - בממשק: ליד לקוח מוכר מופיע תג "⏱ נלמד: 12 דק' (8 ביקורים)" — ראש הצוות רואה שהמערכת מכירה את הלקוחות שלו.

2. **זמני נסיעה אמיתיים בין יעדים:**
   - כל מקטע נסיעה שבוצע נשמר כדגימה: זוג נקודות + שעת יציאה + זמן גוגל החזוי מול הזמן בפועל.
   - המערכת מחשבת **מקדם תיקון** פר-מקטע ופר-חלון שעה (למשל: "בין לקוח א' ללקוח ב' ב-08:00 לוקח בפועל פי 1.15 ממה שגוגל אומר" — כי גוגל לא יודע על החניה, הפריקה, השער האחורי).
   - ETA עתידי = הערכת גוגל × מקדם נלמד (תחום 0.8–1.3 לבטיחות, מופעל רק אחרי 3+ דגימות).

3. **שיפור מתמשך:** כל יום עבודה מזין את המודל. דוח "דיוק חיזוי" שבועי מראה כמה המערכת השתפרה — עוד קלף מכירה מול ברינקס ("המערכת שלכם נהיית חכמה יותר כל יום").

## 4.6 שעון עבודה ולוח בקרה אישי ⏱

ראש הצוות מקבל מעקב שעות מלא — בלי להקליד כלום. המערכת כבר יודעת מתי הוא יצא, איפה היה ומתי חזר.

**איך נמדדות השעות (אוטומטי):**
- **תחילת יום** = יציאה מגיאופנס הסניף (או לחיצה על "התחל סבב" — המוקדם מביניהם).
- **סוף יום** = חזרה לגיאופנס הסניף (או "סיים סבב").
- **הפסקות** מנוכות אוטומטית לפי ההפסקה שנרשמה בפועל (נקודת ההפסקה בסבב + זמן עצירה מזוהה).
- **תיקון ידני זמין תמיד:** אם הגיאופנס פספס (שכח טלפון ברכב, יצא ברגל), ראש הצוות עורך את השעה — כל תיקון מסומן כ"נערך ידנית" בלוג, כך שהנתון נשאר אמין ובר-ביקורת.

**כרטיס "שעון עבודה" בלוח הבקרה:**
| מדד | תצוגה |
|---|---|
| היום | שעון חי שרץ בזמן אמת: `07:12 → כעת · 6:41 שעות` (בפונט Mono, ticker) |
| השבוע | `34:20 / 42:00` — עם טבעת התקדמות זהב |
| החודש | `142:35` + פירוט: רגילות / נוספות |
| מצטבר (מתחילת השימוש) | `487:15 שעות · 62 ימי עבודה` |
| ממוצע יומי | `8:12` — ומגמה מול החודש הקודם |

**פירוט וניתוח:**
- **טבלה יומית:** תאריך | יציאה | חזרה | הפסקה | סה"כ | נוספות | יעדים שבוצעו — עם אפשרות סינון לפי חודש.
- **פילוח זמן היום** (גרף טבעת): נהיגה / שירות אצל לקוחות / המתנה לחלונות זמן / הפסקה. תובנה תפעולית שברינקס תאהב: "23% מהיום זה המתנה — הנה איפה יש כסף על הרצפה".
- **שעות נוספות:** ברירות מחדל ניתנות להגדרה במסך הגדרות (יום תקן, שבוע תקן, סף נוספות). ⚠️ הערכים חייבים להיקבע מול מחלקת השכר של ברינקס וההסכם הקיבוצי החל עליהם — המערכת היא **כלי מעקב ותצוגה, לא מערכת שכר רשמית**. אם יש להם מערכת נוכחות קיימת, בשלב 2 נסנכרן אליה (ולא נתחרה בה).
- **ייצוא:** CSV/PDF של דוח חודשי — כפתור אחד, מוכן לשליחה למנהל.
- **בשלב 2 (דשבורד מנהלים):** אותם נתונים במצטבר לכל הצוותים — מי עמוס, מי מסיים מאוחר באופן קבוע, איפה יש שחיקה.

**ערך מכירה:** זה הופך את האפליקציה מ"עוד כלי ניווט" ל**מערכת התפעול האישית של ראש הצוות** — הוא נכנס אליה גם כדי לראות כמה שעות צבר החודש. שימוש יומיומי = אימוץ = חוזה.

## 5. ארכיטקטורה טכנית

| שכבה | טכנולוגיה | נימוק |
|---|---|---|
| Frontend | React 18 + TypeScript + Vite, PWA | רספונסיבי, ניתן להתקנה, RTL מלא |
| מפה | Google Maps JavaScript API | תצוגת מסלול, מרקרים ממוספרים |
| Backend | Python 3.11 + FastAPI | OR-Tools הוא Python; async; אתה מכיר Flask — FastAPI דומה ומודרני |
| אופטימיזציה | Google OR-Tools (VRPTW solver) | תקן תעשייתי, חינמי |
| נסיעה/גיאוקוד | Google Routes API + Geocoding API + Places Autocomplete | תנועה בזמן אמת, כתובות בעברית |
| OCR חכם | Claude API (vision) | חילוץ כתובות מצילום Zebra — מדויק בהרבה מ-OCR קלאסי בעברית |
| DB | PostgreSQL | מוכן לשלב 2 (ריבוי משתמשים, דשבורד); בפיתוח מקומי — Docker |
| Auth | JWT פשוט (משתמש+סיסמה) | מספיק לפיילוט; SSO בשלב 2 |
| מיקום | Geolocation API (watchPosition) + חישוב Haversine | גיאופנס בצד לקוח |

**עלויות API (לתמחור מול הלקוח):** Google Maps ~200$ קרדיט חודשי חינם; סבב יומי של 25 יעדים ≈ מטריצה אחת + רענונים — עשרות סנטים ליום לראש צוות. Claude API — אגורות לצילום.

## 6. פיצ'רים — טבלת MVP מול שלב 2

**MVP (פיילוט):** קלט משולש (ידני/קובץ/צילום), גיאוקודינג + מסך אימות כתובות, מנוע אופטימיזציה מלא (VRPTW, מטריצה מודעת-זמן, כל האילוצים), זיהוי התנגשויות + הצעות + הסבר מסלול, מפה + רשימה + גרירה ידנית + נעילת יעד, Live Mode עם גיאופנס + Waze + ETA חי, הפסקה חכמה, הוספת יעד עם תצוגה מקדימה "מה יקרה אם", חישוב מחדש חכם, קודי חריגה, סיכום יומי + "זמן שנחסך", **שעון עבודה + לוח בקרה אישי**, היסטוריה + שכפול סבב, SOS, מתג גיוון מסלול, אופליין + סוללה, PWA מותקן, מצב הדגמה.

**כלול ב-MVP גם:** מנוע הלמידה (4.5) — דגימות אוטומטיות, הערכות נלמדות, **תיקון פין נלמד**, **ETA מודע-סיכון (P80)**, פילוח לפי יום בשבוע, וקטגוריות לקוח לפתרון Cold Start (סעיף 8).

**שלב 2:** דשבורד ניהולי (מיקום חי, כל הצוותים, צפי חזרה, שעות מצטברות לכל הצוות), אינטגרציית API ל-Zebra, סנכרון למערכת הנוכחות/שכר של ברינקס, דוחות ואנליטיקה מתקדמים, ריבוי סניפים, התראות למוקד, SSO ארגוני.

## 7. מפת פיתוח (Foundation-First)

| Phase | תוכן | תוצר |
|---|---|---|
| 1 — Foundation | מבנה פרויקט, .cursorrules, Docker+Postgres, סכמת DB מלאה (כולל טבלאות למידה ושעות), שלד FastAPI+React, מערכת עיצוב מלאה, התחברות | האפליקציה עולה, מתחברים, רואים שלד ברמת פרימיום |
| 2 — Data Core | רשומות לקוח קבועות, CRUD יעדים, 3 שיטות קלט, Places Autocomplete, גיאוקודינג, מסך תכנון בוקר | מזינים רשימה מלאה בכל שיטה; לקוח חוזר מזוהה אוטומטית |
| 3 — Route Engine | מטריצה מודעת-זמן (3 חלונות), OR-Tools VRPTW, כל האילוצים, הפסקה כצומת, התנגשויות + הסבר, מפה, עריכה ידנית + נעילה | לוחצים "חשב" ומקבלים מסלול אופטימלי מוסבר |
| 4 — Live Mode + איסוף נתונים | מסך נסיעה, גיאופנס (מתעד arrival/departure + תחילת/סוף יום), Waze, ETA חי, חישוב מחדש חכם, הפסקה, הוספה תוך כדי יום, SOS | יום עבודה מלא מקצה לקצה; כל יום מזין את מנוע הלמידה ואת השעון |
| 5 — Intelligence + Polish | הפעלת הערכות נלמדות, תגי "נלמד", לוח בקרה: שעון עבודה + מצטבר + פילוח זמן + ייצוא, סיכום יומי, היסטוריה+שכפול, מצב גיוון, דוח דיוק, PWA, אנימציות, בדיקות, נגישות | מוצר חכם שמשתפר כל יום — מוכן לפיילוט ולמכירה |

---

## 8. שדרוגי מומחה — ההצעות שלי מעבר לבריף 💡

אלה הדברים שיהפכו את זה ממוצר טוב למוצר שאין לו מתחרה בישראל. סימנתי מה נכנס ל-MVP (זול ליישום, ערך עצום) ומה משאירים לשלב 2.

### נכנסים ל-MVP

**1. תיקון פין נלמד — "איפה החניון האמיתי" 📍 (הכי חשוב ברשימה)**
גוגל שם את הפין על מרכז הכתובת. הרכב המשוריין חונה בכניסה האחורית, ברחוב הצדדי, ברחבת הפריקה. בכל הגעה, ה-GPS מתעד את **הנקודה שבה הרכב באמת נעצר**. אחרי 3 ביקורים המערכת מזיזה את הפין לחציון הנקודות בפועל, ומציגה "📍 נקודת עצירה מדויקת (נלמדה מ-5 ביקורים)". התוצאה: הניווט בוויז מוביל לנקודה הנכונה, הגיאופנס מדויק, וזמני הנסיעה נכונים. **אף מערכת מדף לא עושה את זה** — וזה מגיע בחינם מנתונים שכבר אוספים. → Phase 4-5

**2. ETA מודע-סיכון (P80) — לא לתכנן על הממוצע ⚖️**
"ממוצע" אומר שב-50% מהמקרים תאחר. ליעדים עם דד-ליין המערכת לא משתמשת בהערכה הממוצעת אלא ב**אחוזון 80** מהדגימות ההיסטוריות — כלומר "בזמן שמכסה 8 מתוך 10 מקרים". יעדים ללא דד-ליין מתוכננים על החציון (יעיל). זו הבחנה שמהנדס אופטימיזציה יעריך, והיא ההבדל בין תוכנית שמחזיקה בשטח לתוכנית שקורסת ב-10:30. → Phase 5

**3. קודי חריגה בעצירה — הזהב התפעולי 🏷️**
בסימון "בוצע", אם היה משהו חריג — לחיצה אחת: `לקוח לא היה מוכן` · `המתנה בשער` · `בעיית חניה` · `סגור` · `עומס בסניף`. שלוש שניות מראש הצוות → נכס אדיר לברינקס: "5 הלקוחות שגוזלים לכם הכי הרבה זמן החודש", "בסניף X ממתינים בממוצע 12 דק' בשער". זה הדוח שמוכר את המערכת להנהלה. גם מזין את הלמידה (דגימה עם חריגה מסומנת ולא מזהמת את החציון). → Phase 4

**4. פילוח לפי יום בשבוע 📅**
זמן שירות בסניף בנק ביום ראשון ≠ יום חמישי. פקקים בבוקר יום א' ≠ יום ו'. הדגימות כבר נשמרות עם חותמת זמן — מוסיפים `day_bucket` (א-ה / ו / חג-ערב חג) ל-`leg_samples` ו-`service_samples`. עלות: שדה אחד. רווח: דיוק משמעותי. → Phase 4

**5. פתרון "היום הראשון" (Cold Start) 🥚**
ביום הראשון אין היסטוריה — ואם ההערכות יהיו גרועות, ראש הצוות יאבד אמון ולא יחזור. פתרון: **קטגוריית לקוח** (סניף בנק / כספומט / רשת קמעונאית / עסק פרטי) עם ערכי ברירת מחדל מכוילים שנקבעים בשיחה אחת עם ראש הצוות המנוסה. אחרי 3 ביקורים הערך הנלמד מחליף. הממשק מציג בכנות: "הערכה: 10 דק' (ברירת מחדל)" מול "12 דק' (נלמד · 8 ביקורים)" — כנות בונה אמון. → Phase 2

**6. תצוגה מקדימה "מה יקרה אם" 🔮**
לפני הוספת יעד תוך כדי יום: "הוספת [לקוח] תוסיף 18 דק' ליום, החזרה לסניף תידחה ל-17:24, והדד-ליין של [לקוח ב'] עדיין מובטח ✓". ראש הצוות מחליט **עם מידע**, לא בניחוש. זו הפיכה של המערכת מכלי לשותף. → Phase 4

**7. עמידות שטח: אופליין + סוללה 🔋**
בלי זה המוצר נופל ביום הראשון:
- **תור פעולות אופליין:** סימון "בוצע", קודי חריגה ותיקוני שעות נשמרים ב-IndexedDB ומסתנכרנים כשהרשת חוזרת. ראש הצוות לא מרגיש כלום.
- **המסלול הפעיל נשמר מקומית** — נפילת רשת לא מוחקת את היום.
- **גיאופנס חסכוני:** `watchPosition` בדיוק גבוה רק כשמתקרבים ליעד (< 1 ק"מ, מחושב מול הפין הבא); אחרת דגימה דלילה. אחרת הסוללה נגמרת ב-13:00 והמוצר מת.
- **Wake Lock** במסך הנסיעה (המסך לא נכבה בזמן נהיגה).
→ Phase 4

**8. מצב הדגמה (Demo Mode) 🎬**
מתג שמפעיל סבב לדוגמה עם 18 לקוחות בגוש דן ונתוני היסטוריה מזויפים — כדי שתוכל להדגים למנהלים בברינקס את **המוצר המלא** בלי לחכות לנתונים אמיתיים, ובלי לחשוף לקוחות אמיתיים. כלי המכירה הכי חשוב שלך. → Phase 5

### שלב 2 (אחרי שהפיילוט מוכיח את עצמו)

- **זיהוי אנומליות תפעוליות:** עצירה שנמשכת פי 2.5 מהחציון הנלמד → התראה לדשבורד המנהלים ("בדוק מה קורה"). בחברת שינוע כספים לחריגה כזו יש משמעות תפעולית — אבל **זה לא תחליף למערכת אבטחה**, ויש להציג אותו ככלי תפעולי בלבד ולתאם עם קצין הביטחון שלהם.
- **מקדם מזג אוויר:** גשם כבד מוסיף 10–20% לזמני נסיעה. Weather API + מקדם על המטריצה.
- **דחיית יעד למחר:** "אין זמן היום" → היעד עובר לתכנון של מחר עם דגל עדיפות.
- **דוח ROI לברינקס:** אגרגציה של כל הצוותים — "החודש נחסכו 240 שעות עבודה". זה המסמך שמצדיק את החוזה השנתי.
- **אופטימיזציה רב-רכבית:** אם ברינקס ירצו לחלק יעדים בין כמה צוותים — OR-Tools כבר תומך, המודל בנוי לזה.
- **התאמת קטגוריות אוטומטית** מנתוני החריגות.

---

## 9. סיכונים וניהולם

| סיכון | חומרה | מיטיגציה |
|---|---|---|
| **דיוק גיאוקודינג בישראל** — כתובות לא סטנדרטיות, אזורי תעשייה, מרכזים מסחריים | גבוהה | Places Autocomplete (לא טקסט חופשי) + ציון ביטחון + מסך "אימות כתובות" שמסמן חשודות לפני החישוב + תיקון פין נלמד |
| **סוללה / GPS ברקע בדפדפן** | גבוהה | גיאופנס אדפטיבי + Wake Lock + אישור ידני כרשת ביטחון (הגיאופנס לעולם לא הכרחי לתפקוד) |
| **דיוק נמוך ביום הראשון → אובדן אמון** | גבוהה | ערכי ברירת מחדל לפי קטגוריה + שקיפות ("ברירת מחדל" מול "נלמד") + יום כיול עם ראש הצוות המנוסה |
| **הרגלים: "אני מכיר את המסלול שלי טוב יותר"** | גבוהה | הסבר המסלול ("חוסך 47 דק'") + עריכה ידנית חופשית + המערכת אף פעם לא כופה. אימוץ נבנה באמון, לא בכפייה |
| **עלויות Google API בקנה מידה** | בינונית | קאשינג מטריצות ליעדים חוזרים, 3 מטריצות ולא רענון מתמיד, מכסות ב-GCP. לפיילוט — בתוך הקרדיט החינמי |
| **פרטיות מעקב עובדים** | בינונית-משפטית | ראה סעיף 13 — חובה לתאם מול HR/משפטית של ברינקס |
| **תלות ב-Zebra (אין API)** | בינונית | 3 שיטות קלט; ה-OCR מנטרל את התלות. אינטגרציה = בונוס עתידי, לא תנאי |

---

## 10. הקמת שירותים ומפתחות (לפני Phase 2)

**Google Cloud Platform:**
1. פרויקט חדש → הפעלת חיוב (יש 300$ קרדיט התחלתי + ~200$/חודש חינם ב-Maps).
2. הפעלת 4 APIs: **Routes API** (מטריצה + ניווט), **Geocoding API**, **Places API (New)** (השלמת כתובות), **Maps JavaScript API** (תצוגה).
3. שני מפתחות נפרדים:
   - `GOOGLE_MAPS_SERVER_KEY` — ללא הגבלת referrer, מוגבל ל-APIs של השרת, נשמר רק ב-`.env` של הבקאנד.
   - `GOOGLE_MAPS_BROWSER_KEY` — מוגבל ל-HTTP referrer של הדומיין שלך + Maps JS API בלבד.
4. הגדרת **מכסות יומיות** (Quotas) — הגנה מפני הפתעת חיוב.

**Anthropic:** מפתח API ל-OCR (`ANTHROPIC_API_KEY`) — בשרת בלבד. עלות: אגורות לצילום.

⚠️ **כלל ברזל:** אף מפתח לא נחשף לפרונטאנד מלבד ה-Browser Key המוגבל. כל קריאה אחרת עוברת דרך פרוקסי ב-FastAPI.

---

## 11. פריסה לפיילוט (Deployment)

- **HTTPS הוא חובה, לא המלצה** — Geolocation API ו-Service Worker לא עובדים ב-HTTP. בלי זה אין מוצר.
- **המלצה לפיילוט:** Railway או Render — בקאנד (Docker), Postgres מנוהל עם גיבויים, פרונט כסטטי + CDN, HTTPS אוטומטי, פריסה מ-Git. עלות: ~20-40$/חודש.
- **גיבויים:** יומי אוטומטי + בדיקת שחזור לפני תחילת הפיילוט. אתה מחזיק נתוני לקוחות של ברינקס — אובדן נתונים = סוף הקשר.
- **ניטור:** Sentry (שגיאות) + uptime monitor. אם המערכת נופלת ב-07:00 ואתה מגלה ב-11:00 — נגמר הפיילוט.
- ⚠️ **מיקום הנתונים (Data Residency):** ברינקס עשויה לדרוש שמידע על לקוחותיה יישמר בישראל/באיחוד האירופי ולא בשרתים אמריקאיים. **לברר לפני בחירת ספק הענן** — זה יכול לשנות את כל הארכיטקטורה בדיעבד. שאלה לפגישה.
- **התקנה על מכשיר:** PWA → "הוסף למסך הבית" באנדרואיד/כרום. שווה להכין דף הוראות עברית של 3 שלבים לראש הצוות.

---

## 12. מדדי הצלחה לפיילוט (מה מוכיח שזה עובד)

הגדר את אלה **מראש** מול ברינקס — כך "הצלחה" היא עובדה מדידה, לא ויכוח:

| מדד | יעד |
|---|---|
| חיסכון בזמן סבב | ≥ 10% מול הסדר הידני (המערכת מודדת את זה לבד — `naive_duration` מול `optimized_duration`) |
| דיוק ETA | סטייה חציונית ≤ 8 דקות ליעד, אחרי שבועיים של למידה |
| עמידה בדד-ליינים | ≥ 98% מהיעדים עם אילוץ זמן |
| זמן תכנון בוקר | ≤ 5 דקות (מול המצב הנוכחי) |
| אימוץ | שימוש ב-≥ 90% מימי העבודה בחודש הפיילוט |
| שביעות רצון | ראש הצוות מדרג ≥ 8/10 ורוצה להמשיך |

**משך פיילוט מומלץ:** 4 שבועות — שבועיים ראשונים ללמידה וכיול, שבועיים למדידה נקייה.

---

## 13. נקודות פתוחות לבירור מול הלקוח ⚠️

לקחת לפגישה הבאה:

1. **פרטיות ומעקב עובדים** — האפליקציה מתעדת מיקום ושעות. מעקב GPS אחר עובדים מחייב יידוע ותיאום, ולעיתים הסכמה ו/או תיאום עם ועד העובדים. **חייב לעבור אישור HR/משפטית של ברינקס לפני הפיילוט.** אני לא עורך דין — זו נקודה שהם צריכים לאשר, לא אנחנו. אם אין אישור, אפשר לצמצם: לוותר על מיקום חי בדשבורד ולהשאיר רק זיהוי הגעה מקומי במכשיר.
2. **מיקום שמירת נתונים** — ישראל / אירופה / ארה"ב? (סעיף 11)
3. **גיוון מסלולים** — האם קצין הביטחון דורש אי-חיזוי? (המתג בנוי, צריך רק החלטה)
4. **מערכת נוכחות קיימת** — האם השעון שלנו משלים או מתנגש? האם צריך סנכרון?
5. **Zebra API** — יש? של מי? (מי הספק)
6. **תקן שעות** — יום/שבוע תקן וסף שעות נוספות לפי ההסכם החל עליהם.
7. **קטגוריות לקוח** — אילו סוגי לקוחות יש, ומה זמן הטיפול האופייני לכל סוג? (לכיול Cold Start)
8. **מדריך מיתוג** — קיים? צריך אישור שימוש בשם/לוגו ברינקס במוצר?
9. **מכשיר הפיילוט** — מכשיר חברה או אישי? (סוללה, הרשאות, מדיניות IT)

---

## 14. Phase 0 — לאמת לפני שבונים 🚨

**זה הסעיף הכי חשוב במסמך. הוא נכתב אחרון והוא צריך לקרות ראשון.**

כל המסמך הזה עומד על **הנחה אחת לא בדוקה**: שראש הצוות מסדר את הסבב שלו בצורה לא אופטימלית, ושאלגוריתם יעשה זאת טוב יותר במידה משמעותית. אם ההנחה נכונה — יש לך עסק. אם ראש צוות ותיק עם 15 שנות ניסיון כבר מסדר את היום שלו טוב ב-3% מהאופטימום — **אין מוצר**, ואת זה עדיף לגלות ביום אחד ולא בחודש הרביעי.

### שלוש בדיקות. שבוע אחד. לפני Phase 1.

**1. ספייק האופטימיזציה (יום עבודה אחד):**
- קח **סבב אמיתי אחד** מברינקס — 20 כתובות אמיתיות עם הסדר שראש הצוות בחר בפועל, והזמנים שהיו.
- Notebook פשוט: גיאוקוד → Routes API matrix → OR-Tools. בלי UI, בלי DB, בלי עיצוב. ~150 שורות פייתון.
- **השווה:** הסדר שלו מול הסדר של הפותר.
- 🟢 חוסך ≥8% → יש עסק. בנה.
- 🟡 חוסך 3-8% → יש עסק, אבל **הערך האמיתי הוא במקום אחר** (השעון, החריגות, הדשבורד הניהולי, זיהוי הכתובות). מקד את המוצר מחדש לפני שבונים.
- 🔴 חוסך <3% → עצור. אל תבנה. שנה כיוון או ותר. חסכת לעצמך 4 חודשים.

**2. יום נסיעה ברכב (יום אחד):**
כל תיאור תפעולי במסמך הזה הוא ניחוש שלי מבוסס על תשובות שלך. שב ברכב יום שלם. תגלה 5 דברים שישנו את האפיון: איפה באמת חונים, כמה זמן לוקח לפתוח שער, מתי אין קליטה, מה באמת מעצבן אותם. **חצי מהאפיון הזה ישתנה אחרי היום הזה — וזה בסדר גמור.** זה בדיוק מה שאמור לקרות.

**3. שער ה-IT של ברינקס (שיחה אחת):**
לפני הכל: האם מחלקת ה-IT של ברינקס בכלל **תאשר** אפליקציית צד-שלישי שמחזיקה כתובות לקוחות? בחברת שינוע כספים יש ממשל תאגידי, ולעיתים גם מדיניות גלובלית של הקבוצה. אם התשובה "לא" — הכל כאן תיאורטי. **בדוק את זה בשיחה, לא אחרי 4 חודשי פיתוח.**

---

## 15. הצד המסחרי — להחליט לפני הקוד ⚖️

זה לא נושא טכני, אבל זו ההחלטה עם ההשלכה הכספית הגדולה ביותר בפרויקט — והיא נסגרת **לפני** הפיתוח, לא אחריו.

**מי מחזיק בקניין הרוחני?**
- אם תבנה בתור **work-for-hire** — הקוד שייך לברינקס. קיבלת תשלום פרויקט חד-פעמי, ולא תוכל למכור את אותה מערכת לאיילון, לדואר, לחברות שינוע אחרות, או לכל חברת לוגיסטיקה בעולם.
- אם תשמור על ה-IP ותיתן **רישיון שימוש** — יש לך מוצר. ברינקס היא לקוח ראשון, לא בעלים. אותה מערכת נמכרת שוב ושוב, וההכנסה חוזרת.

**ההפרש בין שתי המילים האלה בחוזה הוא פי 10-50 בשווי לאורך זמן.** חברות גדולות ידרשו בעלות כברירת מחדל — זה סטנדרטי אצלן, ואם לא תעלה את הנושא, הוא ייסגר לרעתך בשקט.

⚠️ **אני לא עורך דין, וזו לא ייעוץ משפטי.** אבל אני יכול להגיד לך בביטחון: **אל תחתום על שום דבר מול ארגון בסדר גודל כזה בלי עורך דין שמתמחה בהסכמי תוכנה.** העלות של שעתיים ייעוץ היא אחוזים בודדים מהסיכון. נושאים לסגור: בעלות IP, רישיון מול העברה, הסכם עיבוד נתונים (אתה מחזיק מידע על לקוחות ברינקס), אחריות ושיפוי, ותנאי הפיילוט (בתשלום או חינם? מה קורה בסופו?).

**עוד שתי נקודות לפני הפיילוט:**
- **מי מחליט בברינקס?** מנהל סניף? מנהל תפעול אזורי? הנהלה? אם אתה מדבר עם מי שאין לו תקציב — אתה בונה עבור אוהד, לא עבור קונה. מפה את שרשרת ההחלטה לפני שאתה משקיע חודשים.
- **תמיכה:** אם המערכת נופלת ב-06:30 והסבב לא יוצא — מי עונה? צריך התחייבות מוגדרת, ולו בסיסית, לפני שראש צוות תלוי בזה כדי לעשות את העבודה שלו.

---

## 16. משמעת היקף — מה באמת נכנס לפיילוט 🎯

המסמך הזה מתאר מוצר מלא. **פיילוט לא צריך מוצר מלא — הוא צריך להוכיח את התזה.** חתוך כך:

### Core — בלי זה אין פיילוט
קלט ידני + קובץ · אימות כתובות · **מנוע האופטימיזציה** · התנגשויות + הסבר · מפה + עריכה ידנית + נעילה · Live Mode + Waze + אישור הגעה · **איסוף כל נתוני הלמידה** · סיכום יומי + "זמן שנחסך" · אופליין בסיסי · PWA.

### Plus — נכנס אחרי שבועיים של פיילוט מוצלח
OCR מצילום · לוח בקרה של השעות · מצב גיוון · ניתוח היסטוריה ודוח דיוק · מצב הדגמה · ייצוא PDF · SOS.

### 🔑 העיקרון הארכיטקטוני החשוב ביותר במסמך
**אסוף נתונים מהיום הראשון. הצג אותם מתי שנוח.**

טבלאות הלמידה, `work_days`, נקודות החניה, קודי החריגה — **חייבות לרוץ מ-Phase 4**, גם אם אף מסך לא מציג אותן. נתון שלא נאסף ביום שלישי אבוד לנצח. אבל הדשבורד שמציג אותם? יכול להיבנות בשבוע השלישי של הפיילוט, כשכבר יש מה להראות בו — וגם יהיה הרבה יותר מרשים, כי הוא יהיה מלא בנתונים אמיתיים במקום ריק.

זו הסיבה שסכמת ה-DB בנויה **מלאה** כבר ב-Phase 1, בזמן שהפיצ'רים נבנים בהדרגה. יסודות מלאים, קומות בהדרגה.

---

# חלק ב' — מערכת העיצוב "Vault" (Premium Security)

לא "עוד אפליקציה כהה עם זהב" — זהות חזותית שנבנתה מהעולם של ברינקס עצמו: כספות, שטרות, חותמות אבטחה, פלדה משוריינת. כל אלמנט עיצובי מגיע משם.

## הרעיון המרכזי + אלמנט החתימה
- **חתימת המוצר: "חוגת הכספת" (Vault Dial)** — טבעת התקדמות מעגלית בראש המסך, מעוצבת כחוגה של כספת: שנתות עדינות מסביב, מחט זהב, ובמרכזה מספר היעדים שהושלמו מתוך הסך. עם כל יעד שמסומן "בוצע" — החוגה **מסתובבת בנקישה** (אנימציית rotate קצרה עם easing מכני + רטט הפטי קל במובייל) כמו נעילת בריח. בסוף היום, כשהסבב הושלם — החוגה "ננעלת" עם אנימציית סגירה וחותם זהב "הסבב הושלם". זה הרגע שראש הצוות מחכה לו כל יום, וזה מה שברינקס יזכרו מהדמו.
- **טקסטורת רקע Guilloché** — הדוגמה הגיליוכית (הקווים המסולסלים העדינים שמודפסים על שטרות כסף) כטקסטורת SVG עדינה מאוד (אטימות 3–4%) על רקעי המסכים הראשיים. רמז תת-סיפי מהעולם של שטרות — אף מתחרה לא ייראה ככה.
- **זהב מטאלי, לא זהב שטוח:** כל אלמנט זהב (כפתור ראשי, מחט החוגה, תגי VIP) מקבל gradient מטאלי עדין (`linear-gradient(135deg, #E8CE7A, #C9A227 55%, #9C7A14)`) + הדגשת קצה עליון בהירה — תחושת מתכת אמיתית, לא צבע.

## צבעים (CSS Custom Properties)
```
--navy-950:#060D1A  --navy-900:#0A1628  --navy-800:#112240  --navy-700:#1A335C
--gold-600:#9C7A14  --gold-500:#C9A227  --gold-400:#DBB94A  --gold-300:#E8CE7A
--steel-400:#8A97AB (פלדה — אלמנטים משניים, אייקונים)
--surface:#0E1B30   --surface-raised:#15274A   --border:#1E3A66  --border-gold:rgba(201,162,39,.25)
--text-primary:#F4F6FB  --text-secondary:#9FB0CC  --text-muted:#5C6E8F
--success:#2FBF71  --warning:#F5A623  --danger:#E5484D  --info:#4A90E8
--glow-gold:0 0 24px rgba(201,162,39,.35)
```
- **Dark-first** (נהיגה, שמש, לילה) עם מתג Light mode (גווני שנהב-פלדה, אותה שפה).
- רקע ראשי: gradient עומק `radial-gradient` מ-navy-900 במרכז ל-navy-950 בקצוות + guilloché — תחושת חלל, לא משטח שטוח.
- זהב = אקשן ראשי + חתימה בלבד. משמעת: מסך ממוצע מכיל אלמנט זהב אחד דומיננטי.

## טיפוגרפיה (זוג פונטים מכוון, לא ברירת מחדל)
- **תצוגה/כותרות: Suez One** — סלאב-סריף עברי כבד וסמכותי, מריח כמו שלט של בנק ותיק. לכותרות מסך, מספר היעד הנוכחי, והחוגה. שימוש במשורה — הוא הקול, לא הרעש.
- **גוף: Heebo** 400/500/700 — קריא, נקי, עברי מצוין.
- **דאטה/שעון: IBM Plex Mono** (או Space Grotesk) עם `font-variant-numeric: tabular-nums` — כל ETA, שעה, ק"מ ומונה. מספרים מיושרים שלא "קופצים" כשמתעדכנים.
- סקאלה: Display 34/Suez, H1 26/Suez, H2 20/Heebo 700, Body 16/Heebo 400, Data 15/Mono, Caption 13/Heebo 500.
- **Live Mode:** שם יעד נוכחי 34/Suez, ETA 44/Mono 700 — נקרא במבט של חצי שנייה ממרחק זרוע.
- מספרים ושעות תמיד LTR בתוך טקסט RTL (`unicode-bidi`).

## אפקטים חזותיים (מדויקים, לא ראוותניים)
- **כרטיסי יעד "לוחית פלדה":** רקע `--surface-raised` עם gradient אנכי עדין (1.5% הבהרה למעלה = תאורה מלמעלה), border 1px, צל רך כפול (ambient + key). פס סטטוס בצד: gradient זהב מטאלי=VIP, כחול=רגיל, ירוק=בוצע, אדום פועם עדין=בסיכון דד-ליין.
- **תג "נלמד" 🧠:** לקוח עם נתוני היסטוריה מקבל תג קטן עם קו-מתאר זהב: "⏱ 12 דק' · 8 ביקורים" — הדרך שבה החוכמה של המערכת נראית לעין.
- **מיקרו-אינטראקציות:** כפתור ראשי — scale(0.97) בלחיצה + glow זהב מתרחב; סימון "בוצע" — צ'קמארק מצויר ב-SVG stroke animation (240ms) ואז הכרטיס מתקפל לרשימת ההושלמו; ETA שמתעדכן — ticker ספרות (הספרה הישנה מחליקה למעלה, החדשה נכנסת מלמטה); הוספת יעד — slide-in עם stagger של 40ms בין כרטיסים.
- **מפה בהתאמה:** Google Maps עם JSON styling כהה מותאם לפלטה (כבישים navy-700, מים navy-950); קו המסלול gradient זהב→ענבר; מרקרים ממוספרים כעיגולי פלדה עם ספרת Mono; המרקר הנוכחי — טבעת זהב עם pulse איטי (2.4s).
- **חוקי משמעת:** אנימציות 160–280ms בלבד, easing `cubic-bezier(0.2, 0.8, 0.2, 1)`; אפס אנימציות דקורטיביות במסכי נסיעה (בטיחות!); כיבוד מלא של `prefers-reduced-motion`.

## ביצועים ומהירות (פרימיום = מהיר)
- טעינה ראשונה < 2 שניות ב-4G: code-splitting לפי מסך, פונטים עם `font-display: swap` + preload, אייקונים כ-SVG sprite (לא ספריית ענק).
- **Optimistic UI:** סימון "בוצע", גרירת סדר, עריכת יעד — מגיבים מיידית בממשק, הסנכרון לשרת ברקע. אפס המתנות מיותרות.
- Skeleton loaders בצורת התוכן האמיתי (לא ספינרים) בכל טעינת נתונים; חישוב מסלול מציג התקדמות חכמה ("מחשב מרחקים… מסדר יעדים… בודק אילוצים…").
- Service Worker: העמוד והנכסים נטענים מקאש — פתיחה שנייה מיידית; המסלול האחרון זמין גם בנפילת רשת רגעית.

## עקרונות UX קריטיים
- **In-vehicle first:** מטרות מגע ≥ 56px ב-Live Mode (כפפות/טלטולים), ניגודיות AA מינימום ו-AAA לטקסט קריטי (ETA, שם יעד), כל פעולה נפוצה בלחיצה אחת מהמסך הראשי.
- RTL מלא (dir="rtl") בכל רכיב, כולל גרירה, מגירות ומפה.
- מצבי מערכת מלאים בעברית: loading (skeleton), empty state עם איור וקריאה לפעולה ("אין עדיין יעדים — הוסף את הראשון"), שגיאות שמסבירות מה קרה ומה עושים (לא "אירעה שגיאה").
- נגישות: פוקוס מקלדת נראה (טבעת זהב), aria-labels בעברית, ניווט מקלדת מלא במסכי התכנון.

---

# חלק ג' — חוקי Cursor (`.cursor/rules/*.mdc`)

> ⚠️ הפורמט הישן `.cursorrules` הוצא משימוש ומתעלמים ממנו ב-Agent mode. החוקים מפוצלים לקבצי `.mdc` ממוקדים בתיקיית `.cursor/rules/` — ראה את הקבצים המצורפים: `core.mdc` (alwaysApply), `design-system.mdc`, `optimizer.mdc`, `time-tracking.mdc`.

```
# BRINKS RouteMaster - Cursor Rules

## LANGUAGE PROTOCOL (STRICT)
- UI text (labels, buttons, toasts, errors shown to user): HEBREW ONLY
- Code (variables, functions, files), comments, console/logs: ENGLISH ONLY
- All UI must support RTL (dir="rtl"). Numbers/times render LTR inline.

## STACK
- Frontend: React 18 + TypeScript + Vite, PWA (vite-plugin-pwa)
- Backend: Python 3.11 + FastAPI + SQLAlchemy + Pydantic v2
- DB: PostgreSQL (docker-compose for local dev)
- Optimization: Google OR-Tools (routing solver, VRPTW)
- Maps: Google Maps JS API (frontend), Routes API + Geocoding + Places (backend proxy)
- OCR: Anthropic Claude API (vision) via backend endpoint
- State: TanStack Query (server state) + Zustand (UI state)
- Styling: CSS Modules + design tokens in src/styles/tokens.css (NO Tailwind)

## OPTIMIZATION ENGINE (non-negotiable principles)
- NEVER implement nearest-neighbor or "sort by distance" as the routing logic. The engine is a full VRPTW solved with OR-Tools RoutingModel.
- Objective = minimize makespan (arrival back at depot), NOT total distance.
- Travel matrix is time-dependent: separate Google Routes API matrices per departure window (morning/midday/afternoon) with trafficModel BEST_GUESS.
- Apply learned correction factors (leg_samples) to matrix values when >= 3 samples exist; clamp factor to [0.8, 1.3].
- Break is a virtual node inside the model, never post-processed onto the result.
- Infeasible input must never fail silently: re-solve with AddDisjunction + penalties, identify the conflicting stops, return structured options for the user to choose.
- Locked stops and already-completed stops are hard constraints on re-optimization.
- Never reorder a route without explicit user approval - always propose, never auto-apply.
- DETERMINISM: set a fixed random seed on the solver. Identical input must produce an identical route every time - a route that changes on every click destroys user trust. The ONLY exception is variance_mode, which is deliberately randomized.

## TIME TRACKING
- work_days is auto-filled from depot geofence exit/entry; manual edits always allowed but must set manually_edited=true and be logged.
- Work-hour standards live in user_settings, never hardcoded. This is a tracking/display tool, not a payroll system of record.
- All times stored UTC, displayed Asia/Jerusalem.
- Max 300 lines per file. Split when approaching limit.
- TypeScript strict mode. No `any`. Pydantic models for every API payload.
- Every API call: loading state, error handling with Hebrew user message, retry where sensible.
- API keys ONLY in .env, accessed via config module. NEVER hardcode. NEVER expose Google/Anthropic keys to frontend - all external API calls go through FastAPI backend proxy.
- All times stored UTC in DB, displayed in Asia/Jerusalem.

## DESIGN SYSTEM ("Vault" - premium security identity)
- Dark-first premium theme. Use ONLY tokens from src/styles/tokens.css. NO Tailwind, NO component libraries (MUI etc.) - custom components only.
- Typography trio: Suez One (Hebrew display/headings), Heebo (body), IBM Plex Mono with tabular-nums (all times/ETAs/numbers).
- Gold is METALLIC: always the gradient token --gold-metallic, never flat color. Reserved for the single primary action per screen + signature elements (Vault Dial, VIP).
- Background: radial navy depth gradient + subtle guilloche SVG texture (3-4% opacity).
- Cards: "steel plate" style - raised surface token, subtle top-light vertical gradient, dual soft shadow, 1px border.
- Micro-interactions: 160-280ms, cubic-bezier(0.2,0.8,0.2,1). Digit ticker for ETA updates. SVG stroke-draw checkmarks. NO decorative animation on driving screens. Respect prefers-reduced-motion.
- Touch targets >= 48px (>= 56px in Live Mode screens). Contrast: AA minimum, AAA for critical text.
- Every screen must handle: loading (content-shaped skeletons, not spinners), empty (Hebrew CTA), error (explains what happened + what to do, in Hebrew).

## PERFORMANCE (premium = fast)
- Route-level code splitting (React.lazy). Fonts: preload + font-display swap.
- Optimistic UI for all frequent actions (mark done, reorder, edit) - update UI instantly, sync in background, rollback on failure with Hebrew toast.
- First load < 2s on 4G budget. No blocking spinners where a skeleton fits.

## WORKFLOW
- Read existing files before editing. Never rewrite a working file wholesale.
- After each task: update project_plan.md (status, completed, next steps).
- Never start the next phase before the current one is verified working.
- Write a brief test note (how to verify) at the end of each task.
```

---

# חלק ד' — Cursor Prompt: Phase 1 (Foundation)

> 🎓 **הסבר לפניך (עברית):** הפרומפט הזה מקים את היסודות בלבד — מבנה, DB, שלד שרת ולקוח, מערכת עיצוב והתחברות. בלי פיצ'רים עדיין. אחרי שתאשר שהכל רץ, נמשיך ל-Phase 2. העתק את הכל מ-📋 ועד הסוף לתוך Cursor.

```
📋 [CONTEXT]:
Project: BRINKS RouteMaster - a premium PWA for a Brinks (cash logistics) team leader.
Purpose: Daily route optimization for 10-25 stops with time windows, VIP priorities,
traffic-aware ETAs, smart breaks, live navigation mode with geofenced arrival detection,
Waze deep-link navigation, and end-of-day analytics. Hebrew RTL UI, dark premium design.
Tech Stack: React 18 + TypeScript + Vite (PWA) | FastAPI + SQLAlchemy | PostgreSQL (Docker)
| Google OR-Tools | Google Maps Platform | Anthropic Claude API (OCR).
Current Phase: 1 of 5 (Foundation)
Last Task: None - fresh project.
Current Status: Empty repository. A .cursorrules file will be provided - follow it strictly.

───────────────────────────────────────────────────

🔨 [TASK]:
Scaffold the complete project foundation: monorepo structure, database, backend skeleton,
frontend skeleton with the full design system, and JWT authentication. NO business
features yet (no routes/stops logic) - foundation only, but visually polished.

Sub-tasks:
1. Create monorepo structure:
   /backend  - FastAPI app (src/main.py, src/core/config.py, src/models/, src/api/,
               src/services/, src/db/session.py, requirements.txt, .env.example)
   /frontend - Vite React-TS app (src/styles/tokens.css, src/styles/global.css,
               src/components/ui/, src/pages/, src/api/client.ts, src/App.tsx)
   Root: docker-compose.yml (postgres:16 + volumes), project_plan.md, README.md, .gitignore

2. Database (SQLAlchemy models + Alembic migration) - includes the LEARNING ENGINE
   tables from day one (populated in Phase 4, used in Phase 5):
   - users: id, username, password_hash, full_name, role ('team_leader'|'manager'), created_at
   - depots: id, name, address, lat, lng  (single Brinks branch, seeded)
   - customers: id, name, normalized_address (UNIQUE with name), lat, lng,
     category ('bank_branch'|'atm'|'retail_chain'|'private_business'|'other'),
     default_service_min INT NULL, learned_service_min FLOAT NULL,
     learned_service_p80 FLOAT NULL, service_sample_count INT DEFAULT 0,
     parking_lat FLOAT NULL, parking_lng FLOAT NULL, parking_sample_count INT DEFAULT 0,
     geocode_confidence FLOAT NULL, notes, created_at
     (persistent customer registry - the memory of the system. category powers
      cold-start defaults; parking_lat/lng is the LEARNED real stopping point,
      which beats Google's address pin; p80 powers risk-aware ETA for deadlines)
   - routes: id, user_id FK, date, status ('planning'|'active'|'completed'),
     departure_time, break_duration_min, break_window_start TIME, break_window_end TIME,
     deadline_buffer_min INT DEFAULT 10, vip_weight FLOAT DEFAULT 1.0,
     variance_mode BOOL, optimized_at TIMESTAMP NULL,
     naive_duration_min INT NULL, optimized_duration_min INT NULL,
     solver_explanation JSONB NULL, created_at
     (naive vs optimized = the "time saved" sales metric; solver_explanation stores
      why this order was chosen - which stops drove the structure)
   - stops: id, route_id FK, customer_id FK NULL, customer_name, address, lat, lng,
     sequence_order INT, locked BOOL, priority ('normal'|'vip'),
     tw_type ('none'|'before'|'after'|'window'), tw_start TIME NULL, tw_end TIME NULL,
     service_duration_min INT DEFAULT 10, service_estimate_source ('default'|'category'|'learned'),
     notes TEXT, status ('pending'|'arrived'|'done'|'skipped'), eta TIMESTAMP NULL,
     actual_arrival TIMESTAMP NULL, actual_departure TIMESTAMP NULL,
     actual_park_lat FLOAT NULL, actual_park_lng FLOAT NULL,
     exception_code ('none'|'not_ready'|'gate_wait'|'parking'|'closed'|'branch_busy'|'other'),
     exception_note TEXT NULL
   - service_samples: id, customer_id FK, stop_id FK, duration_min FLOAT,
     day_bucket ('weekday'|'friday'|'holiday_eve'), hour_bucket INT (0-23),
     exception_code, is_outlier BOOL DEFAULT false, recorded_at
     (each geofence enter->exit measurement: the real time from stepping off the
      truck to driving away. Samples flagged with an exception are excluded from
      the learned median so one bad day never poisons the estimate)
   - leg_samples: id, from_lat, from_lng, to_lat, to_lng, from_customer_id NULL,
     to_customer_id NULL, hour_bucket INT (0-23),
     day_bucket ('weekday'|'friday'|'holiday_eve'),
     predicted_min FLOAT (Google), actual_min FLOAT, recorded_at
     (real vs predicted travel time per leg per time-of-day - powers the learned
      ETA correction factor)
   - work_days: id, user_id FK, date UNIQUE(user_id,date), route_id FK NULL,
     start_at TIMESTAMP NULL, end_at TIMESTAMP NULL, break_min INT DEFAULT 0,
     driving_min INT DEFAULT 0, service_min INT DEFAULT 0, waiting_min INT DEFAULT 0,
     total_min INT NULL, overtime_min INT DEFAULT 0,
     manually_edited BOOL DEFAULT false, edit_note TEXT NULL, created_at
     (the timesheet: auto-filled from depot geofence exit/entry in Phase 4.
      Any manual correction sets manually_edited=true - the record stays auditable)
   - user_settings: id, user_id FK UNIQUE, standard_day_min INT DEFAULT 516,
     standard_week_min INT DEFAULT 2520, geofence_radius_m INT DEFAULT 150,
     theme ('dark'|'light') DEFAULT 'dark'
     (work-hour standards MUST be configurable - final values come from Brinks
      payroll/collective agreement, not hardcoded)
   - route_events: id, route_id FK, type, payload JSONB, created_at  (audit trail,
     powers the future manager dashboard - design for it now)
   Seed script: one team_leader user + user_settings row + one depot
   ("סניף ברינקס", placeholder coords).

3. Backend skeleton:
   - FastAPI with CORS for the Vite dev origin
   - /api/auth/login (JWT, bcrypt), /api/auth/me
   - /api/health
   - Config via pydantic-settings reading backend/.env:
     DATABASE_URL, JWT_SECRET, GOOGLE_MAPS_SERVER_KEY, ANTHROPIC_API_KEY
     (placeholders for now; server-side keys ONLY - never sent to the browser)
   - frontend/.env.example holds VITE_GOOGLE_MAPS_BROWSER_KEY only. This is the single
     sanctioned browser-exposed key, safe only because it is HTTP-referrer restricted
     and limited to the Maps JS API. Both files committed as .env.example only.
   - Structured logging (English), global exception handler returning
     {error: {code, message_he}} so the frontend can show Hebrew messages

4. Frontend skeleton + FULL "Vault" design system (this shell will be demoed to the
   client - it must look like a flagship product, not a scaffold):
   - tokens.css - the complete design language:
     Colors: --navy-950:#060D1A; --navy-900:#0A1628; --navy-800:#112240;
     --navy-700:#1A335C; --gold-600:#9C7A14; --gold-500:#C9A227; --gold-400:#DBB94A;
     --gold-300:#E8CE7A; --steel-400:#8A97AB; --surface:#0E1B30;
     --surface-raised:#15274A; --border:#1E3A66; --border-gold:rgba(201,162,39,.25);
     --text-primary:#F4F6FB; --text-secondary:#9FB0CC; --text-muted:#5C6E8F;
     --success:#2FBF71; --warning:#F5A623; --danger:#E5484D; --info:#4A90E8;
     --gold-metallic: linear-gradient(135deg,#E8CE7A,#C9A227 55%,#9C7A14);
     --glow-gold: 0 0 24px rgba(201,162,39,.35);
     Spacing (4/8/12/16/24/32/48), radius (8/12/16/full),
     shadows: --shadow-card (dual: ambient rgba(0,0,0,.35) 0 8px 24px + key 0 1px 2px),
     transitions: --t-fast:160ms; --t-med:240ms; --ease:cubic-bezier(.2,.8,.2,1)
   - global.css:
     * Depth background: radial-gradient(ellipse at 50% 20%, navy-900, navy-950 75%)
     * Guilloche texture: an inline SVG data-URI of fine interlaced curved lines
       (banknote engraving style) as a fixed background layer at 3-4% opacity
     * Fonts (Google Fonts, preload + font-display:swap): Suez One (display),
       Heebo 400/500/700 (body), IBM Plex Mono 500/700 (all numeric/time data,
       with font-variant-numeric: tabular-nums)
     * html dir="rtl" lang="he"; utility class .num for LTR inline numbers
     * @media (prefers-reduced-motion: reduce) - kill all animation
   - Base UI components (CSS Modules, custom-built, no UI library, RTL):
     * Button: primary = metallic gold gradient + top-edge highlight + gold glow on
       hover + scale(0.97) on press; secondary = navy outline; ghost; danger;
       sizes md/lg (lg >= 56px); built-in loading state
     * Input + FloatingLabel: navy surface, gold focus ring, Hebrew error text
     * Card "steel plate": surface-raised + subtle vertical top-light gradient
       (1.5% lighter at top) + --shadow-card + 1px border; optional side status
       bar variant (gold-metallic / info / success / danger)
     * VaultDial component (THE signature): SVG circular progress ring styled as
       a safe dial - fine tick marks around the circumference, metallic gold
       needle, center shows "X/Y" in Plex Mono. Expose completeStep() that
       rotates the dial one notch with a 240ms mechanical snap (and a subtle
       tick sound-free haptic via navigator.vibrate(10) where supported).
       Demo it on the /app shell with placeholder 0/0 state.
     * Toast system (RTL, slide-in from top, Hebrew), content-shaped Skeleton,
       TopBar (wordmark "RouteMaster" in Suez One + gold shield SVG icon, user menu)
   - Pages:
     * /login: centered steel-plate card over the guilloche depth background,
       shield logo with a one-time subtle gold glow entrance animation (600ms),
       Hebrew labels "שם משתמש","סיסמה","כניסה למערכת", error toast on failure
     * /app: authenticated shell with TopBar + VaultDial (empty state) + Hebrew
       placeholder "ברוך הבא! כאן ייבנה מסך תכנון הסבב", route guard
   - api/client.ts: fetch wrapper with JWT header, 401 -> redirect to login
   - Performance foundation: React.lazy route splitting from day one, font preload,
     Lighthouse performance >= 90 on the login page
   - PWA: vite-plugin-pwa with manifest (name "RouteMaster", Hebrew description,
     theme_color #0A1628, standalone display) and generated placeholder icons

───────────────────────────────────────────────────

⚠️ [CONSTRAINTS]:
1. UI: Hebrew only. Code/comments/logs: English only. RTL everywhere.
2. Follow .cursorrules strictly (max 300 lines/file, tokens-only styling, no Tailwind).
3. NO business logic yet - do not create stops/routes endpoints beyond the models.
4. All secrets via .env; commit .env.example only.
5. Design must look premium out of the box - this shell will be shown to the client.

───────────────────────────────────────────────────

✅ [COMPLETION CRITERIA]:
- [ ] docker-compose up starts PostgreSQL; alembic upgrade head creates all 10 tables
      (users, user_settings, depots, customers, routes, stops, service_samples,
       leg_samples, work_days, route_events)
- [ ] Seed script runs; login with seeded user returns JWT
- [ ] Frontend: login page renders in Hebrew RTL - guilloche depth background,
      steel-plate card, metallic gold button, Suez One heading visible
- [ ] Successful login redirects to /app shell showing TopBar + VaultDial;
      refresh keeps session; logout works
- [ ] Wrong password shows Hebrew error toast
- [ ] VaultDial renders and its notch-rotation animation can be triggered
- [ ] prefers-reduced-motion disables animations (verify via DevTools emulation)
- [ ] Lighthouse: PWA installable; Performance >= 90 on login; no console errors
- [ ] project_plan.md created and filled per template

───────────────────────────────────────────────────

📝 [DOCUMENTATION MANDATE]:
Create project_plan.md with: project overview, tech stack, phase table (1-5 with the
roadmap), Current Status after this task, Completed Tasks, Next Steps (Phase 2: customer
registry + stops CRUD + triple input methods + geocoding), Architecture Decisions
(why FastAPI+OR-Tools, why VRPTW and not nearest-neighbor, why a time-dependent matrix,
why PWA, why backend-proxied API keys, why learning tables exist from day one,
why work-hour standards are configurable rather than hardcoded).

───────────────────────────────────────────────────

🧪 [TESTING]:
1. Run: docker-compose up -d && cd backend && alembic upgrade head && python -m src.seed
2. Run: uvicorn src.main:app --reload  |  Expected: /api/health returns {"status":"ok"}
3. Run: cd frontend && npm run dev  |  Open browser, verify Hebrew RTL login page
4. Login with seeded credentials -> lands on /app shell; check DevTools: no errors,
   JWT present, RTL layout correct, Heebo font loaded.
```

> 🎓 **הסבר:** Phase 1 בונה יסודות בלבד — אבל יסודות **מלאים**: כל 10 הטבלאות (כולל הלמידה והשעות) נוצרות עכשיו, כדי שלא נשכתב DB בעוד שבועיים. מסך הלוגין כבר ברמת מוצר דגל.

---

# חלק ה' — Cursor Prompt: Phase 2 (Data Core)

> 🎓 **מה זה עושה:** מסך תכנון הבוקר — שלוש שיטות הזנה, זיהוי לקוחות חוזרים, ואימות כתובות. בסוף Phase 2 אפשר להזין סבב מלא — עדיין בלי חישוב מסלול.

```
📋 [CONTEXT]:
Project: BRINKS RouteMaster (see project_plan.md for full context).
Current Phase: 2 of 5 (Data Core)
Last Task: Phase 1 complete - monorepo, 10 DB tables, JWT auth, Vault design system,
login + /app shell, VaultDial component, PWA installable.
Current Status: Authenticated shell works. No business features yet.

───────────────────────────────────────────────────

🔨 [TASK]:
Build the morning planning screen: customer registry, stops CRUD, three input methods
(manual / file upload / screenshot OCR), address geocoding + verification.
NO route optimization yet - that is Phase 3.

Sub-tasks:

1. Backend - customer registry service (src/services/customers.py):
   - normalize_address(raw) -> stripped, lowercased, Hebrew-aware normalization
     (unify "רח'"/"רחוב", strip extra whitespace/punctuation)
   - find_or_create_customer(name, address, lat, lng) -> matches an existing customer
     by normalized_address, returns it WITH its learned data
   - Cold-start defaults by category (configurable constants, calibrated with the
     client later): bank_branch=15, atm=8, retail_chain=12, private_business=10, other=10
   - get_service_estimate(customer) -> returns (minutes, source) where source is
     'learned' if service_sample_count >= 3 else 'category' else 'default'

2. Backend - Google Maps proxy (src/services/maps.py) - ALL calls server-side:
   - POST /api/places/autocomplete  (Places API New, language=he, region=il,
     locationBias around the depot) -> [{place_id, description}]
   - POST /api/places/details -> {formatted_address, lat, lng, location_type}
   - POST /api/geocode  (Geocoding API, language=he, region=il) -> includes a
     confidence score derived from location_type: ROOFTOP=1.0,
     RANGE_INTERPOLATED=0.7, GEOMETRIC_CENTER=0.5, APPROXIMATE=0.3
   - Cache geocoding results by normalized address (in-DB via customers table)
     - never pay twice for the same address

3. Backend - OCR endpoint (src/services/ocr.py):
   - POST /api/stops/extract-from-image  (multipart image upload, max 10MB,
     jpeg/png/webp only)
   - Send the image to the Anthropic API (claude-sonnet-4-6) as a base64 image block
     with a system prompt instructing: extract every delivery/pickup destination
     from this Hebrew logistics app screenshot; return ONLY a JSON array, no
     preamble, no markdown fences, schema:
     [{"customer_name": str, "address": str, "time_note": str|null}]
     Return an empty array if none found.
   - Parse defensively: strip markdown fences, json.loads in try/except, validate
     with a Pydantic model, on failure return a Hebrew error telling the user to
     try a clearer photo or enter manually.
   - Each extracted row is then geocoded and returned as an UNCONFIRMED draft -
     the user always reviews before it becomes a stop. Never auto-commit OCR output.

4. Backend - routes & stops CRUD:
   - POST /api/routes  (create today's route: departure_time, break_duration_min,
     break window, variance_mode)
   - GET /api/routes/today
   - POST /api/routes/{id}/stops  (single + bulk), PATCH /api/stops/{id},
     DELETE /api/stops/{id}, PATCH /api/routes/{id}/stops/reorder
   - POST /api/routes/{id}/import-file  (xlsx/csv via pandas/openpyxl; flexible
     Hebrew column detection: שם/לקוח -> name, כתובת -> address, שעה/עד/הערה ->
     time_note; return drafts, never auto-commit)
   - On stop creation: link customer_id via find_or_create_customer and prefill
     service_duration_min + service_estimate_source from get_service_estimate()

5. Frontend - planning screen (/app/plan):
   - Header: date, departure time picker, break duration, variance toggle
   - Three input tabs (Hebrew): "הזנה ידנית" | "העלאת קובץ" | "צילום מסך"
     * Manual: address field with debounced Places Autocomplete dropdown (Hebrew,
       keyboard navigable), customer name, category select, time constraint control
       (segmented: ללא / עד שעה / משעה / בין שעות), VIP toggle, service minutes
       (prefilled + shown source), notes
     * File: drag-and-drop zone + browse, parses to a draft review table
     * Screenshot: camera capture or file, uploads, shows a skeleton while Claude
       extracts ("סורק את הצילום..."), then the draft review table
   - Draft review table: every extracted/imported row is editable before commit,
     with a bulk "הוסף הכל" and per-row remove
   - Stops list: steel-plate cards, drag to reorder (dnd-kit, RTL-aware, 56px grab
     targets), status side bar (gold=VIP), learned badge "⏱ 12 דק' · נלמד · 8 ביקורים"
     vs muted "10 דק' · ברירת מחדל" for new customers
   - Address verification banner: any stop with geocode_confidence < 0.7 is flagged
     "כתובת לא ודאית - בדוק על המפה" with a mini-map pin the user can drag to correct
   - Optimistic UI for add/edit/delete/reorder with rollback + Hebrew toast on failure
   - Empty state: "אין עדיין יעדים - הוסף את הראשון" with a primary gold CTA

───────────────────────────────────────────────────

⚠️ [CONSTRAINTS]:
1. UI Hebrew / code+logs English / RTL. Follow .cursorrules.
2. NO route optimization logic in this phase.
3. OCR and file import produce DRAFTS ONLY - user confirms before commit.
4. Never call Google or Anthropic from the browser - backend proxy only.
5. Debounce autocomplete at 300ms and cache geocodes - API cost discipline.
6. Validate uploads: type, size, and a clear Hebrew error on rejection.

───────────────────────────────────────────────────

✅ [COMPLETION CRITERIA]:
- [ ] Can create today's route and add stops manually with Hebrew autocomplete
- [ ] Uploading an xlsx/csv produces an editable draft table; committing creates stops
- [ ] Uploading a screenshot of a Hebrew address list returns extracted drafts
- [ ] Re-adding a previously used address links to the SAME customer record
- [ ] A low-confidence address shows the verification banner and the pin can be dragged
- [ ] Drag-reorder works in RTL and persists; optimistic UI rolls back on a forced error
- [ ] All time constraint types can be set and persist correctly
- [ ] project_plan.md updated

───────────────────────────────────────────────────

🧪 [TESTING]:
1. Add 5 stops manually with mixed constraints (deadline, window, VIP) - verify DB rows
2. Import a test xlsx with Hebrew headers - verify the draft table and commit
3. Screenshot test: photograph any Hebrew address list - verify extraction
4. Add the same address twice on different routes - verify one customer row, count intact
5. Enter a vague address ("אזור התעשייה חולון") - verify the low-confidence banner
```

> 🎓 **הערה חשובה:** ה-OCR הוא הפיצ'ר שהכי מרשים בדמו, אבל גם הכי שביר. לכן הוא **תמיד** מייצר טיוטה לאישור, אף פעם לא מזין ישירות. אם קורסור מנסה "לחסוך" את שלב האישור — עצור אותו.

---

# חלק ו' — Cursor Prompt: Phase 3 (Route Engine) 🧠

> 🎓 **הלב של המוצר.** זה הפרומפט הכי חשוב במסמך. כאן נבנה המנוע שמצדיק את כל הפרויקט.

```
📋 [CONTEXT]:
Project: BRINKS RouteMaster (see project_plan.md).
Current Phase: 3 of 5 (Route Engine)
Last Task: Phase 2 complete - customers registry, stops CRUD, 3 input methods,
geocoding + verification, planning screen.
Current Status: A full day's stops can be entered. No optimization exists yet.

───────────────────────────────────────────────────

🔨 [TASK]:
Build the optimization engine: time-dependent travel matrices, an OR-Tools VRPTW
solver, conflict detection with options, route map, and manual editing with locks.

CRITICAL: This is NOT a sorting problem. Do not implement nearest-neighbor, greedy
insertion, or distance sorting. Build a real VRPTW model. The objective is to
minimize the arrival time back at the depot (makespan), not total distance.

Sub-tasks:

1. Travel matrix service (src/services/matrix.py):
   - build_matrices(stops, depot, departure_time) -> 3 matrices via Google Routes API
     computeRouteMatrix, one per time bucket:
       morning   = departure_time
       midday    = departure_time + 4h
       afternoon = departure_time + 7h
     Each with trafficModel=TRAFFIC_AWARE_OPTIMAL, travelMode=DRIVE, language=he
   - Batch requests (Routes API limit: 625 elements/request; 25 stops = 676 with
     depot -> split into chunks and merge)
   - pick_matrix(estimated_hour) -> selects the bucket for a given leg
   - apply_learned_factors(matrix) -> multiply each cell by the learned correction
     factor from leg_samples where sample_count >= 3, clamped to [0.8, 1.3].
     (Phase 4 fills leg_samples; this code path must exist now and be a safe no-op
      when there is no data.)
   - Cache matrices per (route_id, departure_time) - do not re-query on every edit

2. Solver (src/services/optimizer.py) - OR-Tools RoutingModel:
   - Nodes: [depot(0), ...stops, break_node]
   - transit_callback(from, to) = travel_min(from,to,estimated_hour) + service_min(from)
   - Time dimension: AddDimension(transit_cb, slack_max=180, capacity=900,
     fix_start_cumul_to_zero=False), start cumul = departure_time
   - Constraints:
       'before' (deadline): CumulVar(idx).SetMax(tw_end - deadline_buffer_min)
       'after':             CumulVar(idx).SetMin(tw_start)
       'window':            CumulVar(idx).SetRange(tw_start, tw_end - buffer)
       break_node:          SetRange(break_window_start, break_window_end),
                            service = break_duration_min, travel to/from = 0
   - Objective: minimize the depot end-node cumul (makespan). Add
     SetGlobalSpanCostCoefficient on the time dimension.
   - VIP: for each VIP stop add a linear cost on its CumulVar weighted by
     route.vip_weight - a soft pull toward earlier, never a hard rule.
   - Locked stops: if stop.locked and sequence_order is set, enforce the position
     via pickup/delivery-style precedence constraints between consecutive locked nodes.
   - Search: FirstSolutionStrategy.PATH_CHEAPEST_ARC ->
     LocalSearchMetaheuristic.GUIDED_LOCAL_SEARCH, time_limit 5s, log_search off
   - Return: ordered stops with per-stop ETA, break placement, depot return time,
     total duration.

3. Infeasibility handling (src/services/conflicts.py) - MUST NOT fail silently:
   - If the solver returns no solution: re-solve with AddDisjunction(node, penalty)
     on every stop with a large penalty, so the solver may drop stops at a cost.
   - Identify which stops were dropped -> those are the conflict drivers.
   - Return a structured response:
     {feasible: false, conflicts: [{stop_ids, reason_he, options: [
        {type:'relax_deadline', stop_id, suggested_time},
        {type:'defer_tomorrow', stop_id},
        {type:'reduce_buffer'},
        {type:'drop_stop', stop_id}]}]}
   - Hebrew reason example: "לא ניתן להגיע ל[א'] עד 10:00 ול[ב'] עד 10:30 - המרחק
     ביניהם 35 דקות."

4. Explainability (part of the solve response):
   - naive_duration_min: simulate the user's ENTERED order through the same matrix
     (this is the honest baseline for the "time saved" claim)
   - optimized_duration_min, saved_min = naive - optimized
   - solver_explanation JSONB: which stops are binding constraints (their ETA is
     within 15 min of their deadline), where the break landed and why
   - Persist all of these on the routes row.

5. API:
   - POST /api/routes/{id}/optimize -> full result or the conflicts response
   - POST /api/routes/{id}/reorder-manual -> user-supplied order; recompute ETAs
     through the matrix WITHOUT re-optimizing, and return constraint warnings
     ("סידור זה יגרום לאיחור אצל [לקוח]")

6. Frontend (/app/plan + /app/route):
   - Primary gold CTA "חשב מסלול אופטימלי" with a staged progress indicator:
     "מחשב מרחקים..." -> "מסדר יעדים..." -> "בודק אילוצים..."
   - Result screen: Google Map (custom dark JSON style matching the Vault palette:
     roads navy-700, water navy-950, POIs muted), route polyline in a gold->amber
     gradient, numbered steel circle markers with Plex Mono digits, depot marker
     with a gold shield icon
   - Ordered stop list beside/below the map: number, name, ETA (Plex Mono), badges
     (VIP gold, deadline, learned), travel time to next
   - Savings banner (the sales moment): "סדר זה חוסך 47 דקות מול הסדר שהזנת ·
     חזרה לסניף ב-16:48"
   - Conflict banner (danger styling) listing options as buttons the user can apply
   - Manual editing: drag to reorder -> instantly shows recomputed ETAs and warnings;
     a lock toggle per stop (gold lock icon) that pins it; "חשב מחדש" respects locks
   - VaultDial in the header now shows 0/N for the planned day

───────────────────────────────────────────────────

⚠️ [CONSTRAINTS]:
1. NEVER nearest-neighbor / distance sorting. Real VRPTW only.
2. Objective = makespan (depot return), NOT distance.
3. Infeasible input -> structured conflicts + options, never a crash or a silent
   "best effort" route that quietly breaks a deadline.
4. The break is a node INSIDE the model, never post-processed.
5. Solver time limit 5s hard - the UI must never hang.
6. All Google calls server-side; cache matrices per route+departure_time.
7. UI Hebrew / code+logs English / RTL. Follow .cursorrules.

───────────────────────────────────────────────────

✅ [COMPLETION CRITERIA]:
- [ ] 15 real Tel Aviv-area addresses with no constraints -> a sensible optimized
      order that beats the entered order; savings banner shows a positive number
- [ ] Adding a hard deadline to the geographically-last stop visibly restructures
      the route (this proves it is not distance sorting)
- [ ] A VIP stop moves earlier when vip_weight is raised
- [ ] Two mutually impossible deadlines -> conflicts response with Hebrew reason
      and applicable options (not a crash)
- [ ] The break lands inside its window and at a sensible point in the route
- [ ] Locking a stop to position 3 and re-optimizing keeps it at position 3
- [ ] Manual drag shows updated ETAs and a warning when a deadline breaks
- [ ] Solve completes < 8s end-to-end for 25 stops
- [ ] project_plan.md updated

───────────────────────────────────────────────────

🧪 [TESTING]:
1. Seed a fixture of 15 real addresses; run optimize; compare naive vs optimized
2. Unit test the solver with a synthetic 5-node case where the correct answer is
   known by hand - assert exact order
3. Add an impossible constraint pair; assert feasible=false and non-empty options
4. Lock a stop; re-run; assert position preserved
5. Verify the API call count per solve (should be 3 matrix calls, not per-leg calls)
```

> 🎓 **הערה קריטית:** קורסור **ינסה** לקצר פה ולכתוב מיון פשוט — זו הדרך הקלה. אם אתה רואה בקוד `sort`, `nearest`, או לולאה שבוחרת את המינימום הבא — עצור והצג לו שוב את סעיף ה-CONSTRAINTS. זה ההבדל בין המוצר שלך לאקסל.

---

# חלק ז' — Cursor Prompt: Phase 4 (Live Mode + איסוף נתונים)

> 🎓 **מה זה עושה:** הופך את התוכנית ליום עבודה אמיתי — וכל יום עבודה מזין את המוח של המערכת.

```
📋 [CONTEXT]:
Project: BRINKS RouteMaster (see project_plan.md).
Current Phase: 4 of 5 (Live Mode + data capture)
Last Task: Phase 3 complete - VRPTW engine, conflicts, map, manual editing, locks.
Current Status: An optimal route can be produced and edited. Nothing tracks the
actual day yet, and the learning tables are still empty.

───────────────────────────────────────────────────

🔨 [TASK]:
Build the in-vehicle driving experience, geofenced arrival detection, automatic
capture of learning samples and work hours, smart re-optimization, and offline
resilience.

Sub-tasks:

1. Live Mode screen (/app/live) - designed to be read at a glance while driving:
   - Current stop name in Suez One 34px; ETA in Plex Mono 44px
   - Full-width gold "נווט בוויז" button (>= 56px) ->
     window.location.href = `waze://?ll=${lat},${lng}&navigate=yes`
     with an https://waze.com/ul?ll=... fallback if the app is not installed.
     Navigate to parking_lat/lng when learned, otherwise the address pin.
   - Secondary: "פרטי לקוח" drawer (phone, notes, learned service time)
   - VaultDial header: X/Y done, notch-rotates on each completion
   - Next-up preview: the following stop + travel time
   - Progress list (collapsible): done / current / upcoming
   - Screen Wake Lock while in Live Mode; release on exit

2. Geofencing (src/hooks/useGeofence.ts) - battery-aware:
   - navigator.geolocation.watchPosition with adaptive accuracy: enableHighAccuracy
     ONLY when Haversine distance to the next stop < 1km; otherwise low accuracy
     with a longer maximumAge
   - Enter event: distance <= user_settings.geofence_radius_m (default 150) for
     >= 30 consecutive seconds -> set stop.status='arrived', actual_arrival=now,
     record actual_park_lat/lng from the GPS fix, and show the confirmation prompt
     "הגעת ל[לקוח]? סמן בוצע" (large, one tap)
   - Exit event: distance > radius for >= 60s after status='arrived' ->
     actual_departure=now, status='done'
   - The geofence NEVER blocks: manual "סמן בוצע" and "דלג" are always available,
     and everything works if GPS is denied or unavailable (degraded, not broken)
   - Depot geofence: exit in the morning -> work_days.start_at; entry at end of
     day -> work_days.end_at

3. Exception codes:
   - On "סמן בוצע", an optional one-tap chip row (Hebrew): לקוח לא היה מוכן /
     המתנה בשער / בעיית חניה / סגור / עומס בסניף / אחר + free note
   - Stored on stops.exception_code; the resulting service sample is flagged so it
     does not poison the learned median

4. Learning capture (src/services/learning.py) - runs on each stop completion:
   - service_samples: duration = actual_departure - actual_arrival, with
     day_bucket + hour_bucket + exception_code + is_outlier
     (is_outlier = exception_code != 'none' OR duration > 3x current median)
   - leg_samples: on departure from stop N toward N+1, record predicted_min (what
     the matrix said) vs actual_min (measured on arrival), with buckets
   - parking point: append the fix to the customer's parking samples; once
     parking_sample_count >= 3, set parking_lat/lng to the median of the samples
     (median, not mean - one bad fix must not move the pin)
   - Recompute customer.learned_service_min (EWMA-weighted median over non-outlier
     samples) and learned_service_p80 (80th percentile) after each new sample

5. Smart re-optimization:
   - Triggers: add stop mid-day, skip/cancel a stop, cumulative delay > 15 min,
     manual request
   - Completed stops are frozen as history; solve only the remainder, starting from
     the CURRENT GPS position and CURRENT time
   - ALWAYS propose, never auto-apply:
     "זוהה עיכוב של 22 דק'. סידור מחדש יחסוך 18 דק' וישמור על הדד-ליין של [לקוח].
      להחיל?" [החל] [השאר כמו שהוא]
   - "What-if" preview before adding a mid-day stop:
     "הוספת [לקוח] תוסיף 18 דק', חזרה לסניף תידחה ל-17:24, הדד-ליין של [ב'] מובטח ✓"
     Only commit on confirmation.

6. Offline resilience:
   - IndexedDB queue (idb-keyval) for: mark done, exception codes, manual time edits
   - The active route + its ETAs are cached locally on route start
   - Sync on reconnect with a Hebrew status pill: "לא מקוון · 3 פעולות ממתינות"
   - Service worker: app shell + assets cached; API calls network-first with a
     cached fallback for the active route

7. SOS: a persistent small red button in Live Mode -> confirm sheet ->
   tel: link to a configurable control-center number (user_settings). Logged to
   route_events. (Presentational + dialing only - not a security system.)

───────────────────────────────────────────────────

⚠️ [CONSTRAINTS]:
1. The geofence is an assistant, never a gate. Every automatic action has a manual
   equivalent. GPS denied -> the app still works fully.
2. Never auto-reorder without explicit user approval.
3. Battery discipline: high-accuracy GPS only within 1km of the next stop.
4. No blocking spinners in Live Mode - the driver must never wait on the network.
5. Median (not mean) for learned values; outliers flagged and excluded.
6. UI Hebrew / code+logs English / RTL. Follow .cursorrules.

───────────────────────────────────────────────────

✅ [COMPLETION CRITERIA]:
- [ ] Live Mode is readable at arm's length; Waze opens to the correct coordinates
- [ ] Simulated GPS movement (DevTools sensors) triggers arrival prompt, then exit
      completes the stop and advances to the next
- [ ] service_samples and leg_samples rows are created with correct buckets
- [ ] After 3 visits to the same customer, learned_service_min and parking_lat/lng
      are populated and the learned badge appears
- [ ] work_days.start_at/end_at are captured from the depot geofence
- [ ] Airplane mode: marking stops still works; reconnecting syncs the queue
- [ ] Delay > 15 min triggers a re-optimization PROPOSAL (not an auto-change)
- [ ] Adding a mid-day stop shows the what-if preview before committing
- [ ] Battery: high-accuracy GPS confirmed off when far from the next stop
- [ ] project_plan.md updated

───────────────────────────────────────────────────

🧪 [TESTING]:
1. Simulate a full day with DevTools geolocation overrides along a real route
2. Verify DB after: samples, buckets, work_days, parking points
3. Repeat the same customer 3 times -> assert learned values and the badge
4. Airplane mode mid-route -> mark 3 stops -> reconnect -> assert all synced once
5. Deny location permission -> assert the app is fully usable manually
```

---

# חלק ח' — Cursor Prompt: Phase 5 (Intelligence + Polish → MVP)

> 🎓 **הפאזה שהופכת את זה למוצר שנמכר.** כאן הלמידה מופעלת, לוח הבקרה נבנה, והמוצר נארז.

```
📋 [CONTEXT]:
Project: BRINKS RouteMaster (see project_plan.md).
Current Phase: 5 of 5 (Intelligence + Polish)
Last Task: Phase 4 complete - Live Mode, geofencing, learning capture, offline,
re-optimization, work day capture.
Current Status: Full day works end to end; learning tables now fill with real data
but the estimates are not yet consumed by the planner. No dashboard yet.

───────────────────────────────────────────────────

🔨 [TASK]:
Activate the learning engine in planning, build the personal dashboard (work hours +
analytics), daily summary, history, demo mode, and production polish.

Sub-tasks:

1. Activate learning in the planner:
   - When creating a stop for a known customer: service_duration_min comes from
     learned_service_min (source='learned') once sample_count >= 3
   - RISK-AWARE ETA: in the solver, stops with a deadline ('before'/'window') use
     learned_service_p80 instead of the median; non-deadline stops use the median.
     Document this in a code comment: we plan deadlines on the realistic worst case,
     not the average.
   - Matrix: apply_learned_factors is now live (leg_samples, bucketed by hour + day)
   - Navigation and geofence use parking_lat/lng where learned
   - UI badges: "⏱ 12 דק' · נלמד · 8 ביקורים" and "📍 נקודת עצירה מדויקת (5 ביקורים)"

2. Personal dashboard (/app/dashboard):
   - Live work clock card: "07:12 → כעת · 6:41 שעות" (Plex Mono, ticking, digit
     ticker animation)
   - Cards: this week (X:XX / standard, gold progress ring), this month (regular +
     overtime split), cumulative ("487:15 שעות · 62 ימי עבודה"), daily average with
     a trend vs last month
   - Day composition donut: driving / service / waiting / break, with the insight
     line: "23% מהיום המתנה"
   - Daily table: date | out | in | break | total | overtime | stops done; month filter
   - Manual time correction: editable start/end, sets manually_edited=true + note,
     shown with a small "נערך" marker
   - Standards from user_settings (never hardcoded); settings screen to change them
   - Export: CSV + PDF of the monthly report (one tap)

3. Daily summary (on depot arrival) - the sales moment:
   - VaultDial locks with the seal animation + "הסבב הושלם"
   - Stats: stops done, planned vs actual finish, km, "חסכת היום 47 דקות"
   - Exceptions of the day; top time-wasting customers this month
   - "שכפל סבב זה למחר" button

4. History & analytics (/app/history):
   - Route list by date; open any past route (map + list + actuals vs plan)
   - Duplicate to a new route
   - Weekly "accuracy report": median ETA error trend over time - proves the system
     is learning ("דיוק החיזוי השתפר ב-38% מאז תחילת השימוש")

5. Variance mode (security):
   - When route.variance_mode: request 5 solutions from the solver, keep those
     within 7% of the optimum, prefer one whose order differs most from the last
     7 days for the same customer set, pick randomly among the remainder
   - UI note: "מצב גיוון פעיל · +4 דק'"

6. Demo mode:
   - A settings toggle that seeds a realistic demo route (18 Gush Dan customers)
     with fabricated history so learned badges, dashboard, and analytics are all
     populated. Clearly marked "מצב הדגמה" in the TopBar. Isolated from real data.

7. Production polish:
   - Full a11y pass: keyboard navigation, visible gold focus rings, Hebrew
     aria-labels, contrast audit (AA min / AAA for ETA + stop name)
   - Error boundaries + Sentry; Hebrew empty/error states everywhere
   - Tests: pytest for optimizer/learning/conflicts services (the critical logic);
     Vitest for hooks; one Playwright happy-path E2E
   - Performance: Lighthouse >= 90 on all main screens; route-level code splitting
   - README: setup, env vars, deploy steps, and a 3-step Hebrew "install on your
     phone" guide for the team leader

───────────────────────────────────────────────────

⚠️ [CONSTRAINTS]:
1. Learned values only when sample_count >= 3; always show the source honestly.
2. P80 for deadline stops, median otherwise - do not blur this distinction.
3. Work-hour standards from user_settings only. This is a tracking tool, not a
   payroll system of record - do not claim otherwise in any UI copy.
4. Demo data must never mix with real data (flag + separate route rows).
5. UI Hebrew / code+logs English / RTL. Follow .cursorrules.

───────────────────────────────────────────────────

✅ [COMPLETION CRITERIA]:
- [ ] A customer with 3+ visits plans with its learned time and shows the badge
- [ ] A deadline stop is planned with p80 (verify in the solver input log)
- [ ] Navigation targets the learned parking point when available
- [ ] Dashboard: live clock ticks, weekly/monthly/cumulative totals correct against
      seeded work_days, donut matches the sums
- [ ] Manual time edit persists, is marked, and is logged
- [ ] CSV + PDF export produce a correct monthly report
- [ ] Daily summary triggers on depot arrival with the dial seal animation
- [ ] Duplicating a past route creates an editable new route
- [ ] Variance mode produces a different order across runs, within 7% of optimum
- [ ] Demo mode populates a full, believable product state
- [ ] Lighthouse >= 90 (perf + a11y) on plan, live, dashboard; tests pass
- [ ] project_plan.md updated: MVP COMPLETE

───────────────────────────────────────────────────

🧪 [TESTING]:
1. Seed 3 visits for one customer -> verify learned planning + badge
2. Verify p80 > median in the solver input for a deadline stop
3. Seed a month of work_days -> verify every dashboard number by hand
4. Export CSV -> open in Excel -> verify columns and totals
5. Run variance mode 5 times -> assert different orders, all within 7%
6. Full Playwright E2E: login -> plan -> optimize -> live -> complete -> summary
```

---

# חלק ט' — אחרי ה-MVP: הדרך למכירה

1. **יום כיול (לפני הפיילוט):** שב שעה עם ראש הצוות המנוסה ביותר — כייל את ערכי ברירת המחדל לפי קטגוריה, ואת רדיוס הגיאופנס. שעה אחת שמצילה את השבוע הראשון.
2. **שבוע 1-2 של פיילוט:** למידה וכיול. אל תציג מספרים להנהלה עדיין — המערכת עוד לומדת.
3. **שבוע 3-4:** מדידה נקייה מול המדדים בסעיף 12.
4. **מצגת ההנהלה:** לא "יש לנו אפליקציה יפה" אלא "ראש הצוות חסך 47 דקות ביום × 21 ימים × עלות שעת עבודה = X ₪ לצוות לשנה. יש לכם N צוותים." — המערכת מייצרת את המספרים האלה לבד. זו מכירה מבוססת ROI, וזו הדרך היחידה למכור לחברה בסדר גודל כזה.
5. **המספר שמוכר את שלב 2:** פילוח ההמתנה. "23% מזמן העבודה זה המתנה שאפשר לתכנן טוב יותר" — זה מה שפותח את הדלת לדשבורד הניהולי ולחוזה הרחב.

**המלצה מסחרית:** תמחר לפי ערך, לא לפי שעות פיתוח. אם המערכת חוסכת 40 דקות ביום לצוות, וברינקס מפעילה עשרות צוותים — החיסכון השנתי הוא במאות אלפי שקלים. מודל SaaS לפי צוות/חודש + דמי הטמעה מתיישב עם התקציבים של ארגון כזה, ומייצר לך הכנסה חוזרת במקום פרויקט חד-פעמי. (אני לא יועץ עסקי — כדאי לאמת את המודל מול מישהו שמכיר את השוק הזה.)
